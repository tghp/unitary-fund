---
export interface Props {
  style: 'one' | 'two' | 'three';
}

const { style } = Astro.props;
---

<div
  class:list={[
    style === 'one' && 'text-xl md:text-2xl',
    style === 'two' && 'text-lg',
  ]}
>
  <slot />
</div>
