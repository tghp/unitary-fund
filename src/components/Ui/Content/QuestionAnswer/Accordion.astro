---
import Accordion from '~/components/Ui/Content/QuestionAnswer/Accordion';
---

<Accordion className="flex flex-col pb-4" client:load>
  <slot />
</Accordion>
<style lang="scss" is:global>
  .question-answer-accordion {
    h2 {
      p {
        @apply m-0;
      }

      * {
        @apply pointer-events-none;
      }

      ~ :not(h2) {
        @apply hidden;
      }
    }
  }
</style>
