---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class:list={['md:h-[calc(100vh-96px-1.5rem)] mt-24 md:mt-0 flex flex-col md:text-lg md:ml-10 ', className]}>
  <slot />
</div>
