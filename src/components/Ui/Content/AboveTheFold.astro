---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div
  class:list={[
    'md:min-h-[calc(100vh-var(--header-height)-theme(spacing.6))] mt-24 md:mt-0 flex flex-col md:text-lg md:ml-10 ',
    className,
  ]}
>
  <slot />
</div>
