---
export interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<div class:list={['number-points']}>
  {title && <h2 class="md:mb-8 md:ml-0 ml-4 font-bold text-xl md:text-4xl">{title}</h2>}
  <slot />
</div>
